import { Typography, colors } from '@mui/material';

export default function Logo() {
  return (
    <Typography
      fontWeight={900}
      color={colors.blue[600]}
      component="h4"
      variant="h4"
    >
      Ꝏ.TAREFAS
    </Typography>
  );
}
